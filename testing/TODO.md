# Testing TODOs

## Completed

Threshold tests have been moved to `integration/usageThreshold/`. MAX mode detection and spending guard tests live in their own suites under `integration/maxModeDetection/` and `integration/spendingGuard/` respectively.
